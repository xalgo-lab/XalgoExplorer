# xAlgo Explorer v0.1.2-candidate

## GitHub Release Notes

`v0.1.2-candidate` focuses on replacing the file area with native macOS AppKit controls while keeping the existing SwiftUI shell and product behavior unchanged.

### Highlights

- Migrated the file area to native AppKit controls:
  - List mode now uses `NSTableView`.
  - File and preview modes now use `NSCollectionView`.
- Kept `ExplorerModel` as the single source of truth for paths, selection, sorting, drag/drop rules, clipboard behavior, and pane focus.
- Improved file list stability for column resizing, header sorting, keyboard selection, and three-pane focus routing.
- Fixed keyboard selection scrolling so the active selection is automatically brought into view.
- Fixed AppKit drag pasteboard handling for internal drags, multi-file drags, and Finder-style copy/move behavior.
- Added regression coverage for AppKit file-area selection, drag pasteboard behavior, keyboard scrolling, and candidate release metadata.

### Candidate Assets

- App bundle: `xAlgo Explorer.app`
- DMG: `xAlgo_Explorer_v0.1.2-candidate_aarch64.dmg`
- SHA256:

```text
10433f659dc514c3015d1837dd85c18caf572f2afd5fc9cc6c4ba922f774fdc1
```

### Validation

- `swift test`: passed, `55 tests, 0 failures`
- `make dmg`: passed
- `codesign --verify --deep --strict --verbose=2 "dist/xAlgo Explorer.app"`: passed
- `hdiutil verify "dist/xAlgo_Explorer_v0.1.2-candidate_aarch64.dmg"`: passed

### Compatibility

- Apple Silicon macOS
- macOS 14 or later

### Notes

This is a candidate build. It is intended for validation of the AppKit-backed file area before promoting the branch into `main`.

## 多国语言更新说明

### 简体中文

`v0.1.2-candidate` 主要将文件区切换为 macOS 原生 AppKit 控件，同时保持外层 SwiftUI 界面结构和既有功能定义不变。

- 列表模式改用 `NSTableView`。
- 文件/预览模式改用 `NSCollectionView`。
- `ExplorerModel` 仍然是路径、选择、排序、拖拽规则、剪贴板和窗口焦点的唯一业务状态源。
- 改进列表列宽、表头排序、键盘选择、焦点切换和三窗口文件区稳定性。
- 修复键盘向下选择文件时，选中项不会自动滚入可见区域的问题。
- 修复 AppKit 拖拽 pasteboard 写入，支持内部拖拽、多文件拖拽和 Finder 风格复制/移动规则。
- 增加 AppKit 文件区、拖拽 pasteboard、键盘滚动、选择行为和候选版本元数据的回归测试。

### 繁體中文

`v0.1.2-candidate` 主要將檔案區切換為 macOS 原生 AppKit 控件，同時保持外層 SwiftUI 介面結構和既有功能定義不變。

- 列表模式改用 `NSTableView`。
- 檔案/預覽模式改用 `NSCollectionView`。
- `ExplorerModel` 仍然是路徑、選取、排序、拖放規則、剪貼簿和視窗焦點的唯一業務狀態來源。
- 改進列表欄寬、表頭排序、鍵盤選取、焦點切換和三視窗檔案區穩定性。
- 修復鍵盤向下選取檔案時，選中項不會自動捲入可視區域的問題。
- 修復 AppKit 拖放 pasteboard 寫入，支援內部拖放、多檔案拖放和 Finder 風格複製/移動規則。
- 增加 AppKit 檔案區、拖放 pasteboard、鍵盤捲動、選取行為和候選版本元資料的回歸測試。

### English

`v0.1.2-candidate` migrates the file area to native macOS AppKit controls while preserving the outer SwiftUI interface and existing product behavior.

- List mode now uses `NSTableView`.
- File and preview modes now use `NSCollectionView`.
- `ExplorerModel` remains the single source of truth for paths, selection, sorting, drag/drop rules, clipboard state, and pane focus.
- Improved list column resizing, header sorting, keyboard selection, focus routing, and three-pane file-area stability.
- Fixed keyboard selection scrolling so the active selection is automatically brought into view.
- Fixed AppKit drag pasteboard writing for internal drags, multi-file drags, and Finder-style copy/move rules.
- Added regression coverage for the AppKit file area, drag pasteboard behavior, keyboard scrolling, selection behavior, and candidate metadata.

### 日本語

`v0.1.2-candidate` では、外側の SwiftUI インターフェイスと既存の機能定義を維持したまま、ファイル領域を macOS ネイティブの AppKit コントロールへ移行しました。

- リスト表示は `NSTableView` を使用します。
- ファイル/プレビュー表示は `NSCollectionView` を使用します。
- `ExplorerModel` は、パス、選択、並び替え、ドラッグ&ドロップ規則、クリップボード状態、ペインのフォーカスの唯一の情報源として維持されます。
- リストの列幅調整、ヘッダー並び替え、キーボード選択、フォーカス制御、3 ペインのファイル領域の安定性を改善しました。
- キーボード選択時に、現在の選択項目が自動的に表示領域へスクロールされない問題を修正しました。
- 内部ドラッグ、複数ファイルドラッグ、Finder 風のコピー/移動規則に対応する AppKit drag pasteboard 書き込みを修正しました。
- AppKit ファイル領域、drag pasteboard、キーボードスクロール、選択動作、候補版メタデータの回帰テストを追加しました。

### 한국어

`v0.1.2-candidate`는 외부 SwiftUI 인터페이스와 기존 기능 정의를 유지하면서 파일 영역을 macOS 네이티브 AppKit 컨트롤로 전환합니다.

- 목록 모드는 `NSTableView`를 사용합니다.
- 파일/미리보기 모드는 `NSCollectionView`를 사용합니다.
- `ExplorerModel`은 경로, 선택, 정렬, 드래그 앤 드롭 규칙, 클립보드 상태, 창 포커스의 단일 상태 소스로 유지됩니다.
- 목록 열 너비 조절, 헤더 정렬, 키보드 선택, 포커스 라우팅, 3개 창 파일 영역 안정성을 개선했습니다.
- 키보드로 선택할 때 현재 선택 항목이 자동으로 보이는 영역으로 스크롤되지 않던 문제를 수정했습니다.
- 내부 드래그, 다중 파일 드래그, Finder 스타일 복사/이동 규칙을 위한 AppKit drag pasteboard 쓰기를 수정했습니다.
- AppKit 파일 영역, drag pasteboard, 키보드 스크롤, 선택 동작, 후보 버전 메타데이터에 대한 회귀 테스트를 추가했습니다.

### Français

`v0.1.2-candidate` migre la zone de fichiers vers des contrôles AppKit natifs de macOS tout en conservant l'interface SwiftUI externe et le comportement produit existant.

- Le mode liste utilise désormais `NSTableView`.
- Les modes fichiers et aperçu utilisent désormais `NSCollectionView`.
- `ExplorerModel` reste l'unique source d'état pour les chemins, la sélection, le tri, les règles de glisser-déposer, le presse-papiers et le focus des panneaux.
- Amélioration du redimensionnement des colonnes, du tri par en-tête, de la sélection au clavier, du routage du focus et de la stabilité de la zone de fichiers en trois panneaux.
- Correction du défilement lors de la sélection au clavier afin que l'élément actif soit automatiquement visible.
- Correction de l'écriture du pasteboard AppKit pour les glissements internes, les glissements multi-fichiers et les règles copier/déplacer de type Finder.
- Ajout de tests de régression pour la zone de fichiers AppKit, le pasteboard de glisser-déposer, le défilement clavier, la sélection et les métadonnées de version candidate.

### Deutsch

`v0.1.2-candidate` migriert den Dateibereich auf native macOS-AppKit-Steuerelemente, während die äußere SwiftUI-Oberfläche und das bestehende Produktverhalten erhalten bleiben.

- Der Listenmodus verwendet jetzt `NSTableView`.
- Datei- und Vorschaumodus verwenden jetzt `NSCollectionView`.
- `ExplorerModel` bleibt die einzige Quelle für Pfade, Auswahl, Sortierung, Drag-and-drop-Regeln, Zwischenablage und Pane-Fokus.
- Verbesserte Stabilität für Spaltenbreiten, Kopfzeilensortierung, Tastaturauswahl, Fokussteuerung und den Dateibereich im Drei-Pane-Modus.
- Behoben: Bei Tastaturauswahl wird das aktive Element nun automatisch in den sichtbaren Bereich gescrollt.
- Behoben: AppKit-drag-pasteboard-Schreiben für interne Drags, Mehrfachdatei-Drags und Finder-artige Kopieren-/Verschieben-Regeln.
- Regressionstests für AppKit-Dateibereich, Drag-pasteboard, Tastatur-Scrolling, Auswahlverhalten und Kandidaten-Metadaten ergänzt.

### Español

`v0.1.2-candidate` migra el área de archivos a controles AppKit nativos de macOS, manteniendo intactas la interfaz SwiftUI externa y el comportamiento existente del producto.

- El modo lista ahora usa `NSTableView`.
- Los modos de archivos y vista previa ahora usan `NSCollectionView`.
- `ExplorerModel` sigue siendo la única fuente de estado para rutas, selección, ordenación, reglas de arrastrar y soltar, portapapeles y foco de paneles.
- Mejora de estabilidad para el ancho de columnas, ordenación por encabezado, selección con teclado, foco y área de archivos de tres paneles.
- Corregido el desplazamiento al seleccionar con teclado para que el elemento activo se muestre automáticamente.
- Corregida la escritura del pasteboard de AppKit para arrastres internos, arrastres de varios archivos y reglas de copiar/mover estilo Finder.
- Añadidas pruebas de regresión para el área de archivos AppKit, drag pasteboard, desplazamiento con teclado, selección y metadatos de versión candidata.

### Português

`v0.1.2-candidate` migra a área de arquivos para controles AppKit nativos do macOS, preservando a interface SwiftUI externa e o comportamento existente do produto.

- O modo lista agora usa `NSTableView`.
- Os modos de arquivos e pré-visualização agora usam `NSCollectionView`.
- `ExplorerModel` continua sendo a única fonte de estado para caminhos, seleção, ordenação, regras de arrastar e soltar, área de transferência e foco dos painéis.
- Melhorias em redimensionamento de colunas, ordenação por cabeçalho, seleção pelo teclado, foco e estabilidade da área de arquivos em três painéis.
- Corrigida a rolagem ao selecionar pelo teclado para que o item ativo seja exibido automaticamente.
- Corrigida a gravação do pasteboard de drag do AppKit para drags internos, drags de vários arquivos e regras de copiar/mover no estilo Finder.
- Adicionados testes de regressão para a área de arquivos AppKit, drag pasteboard, rolagem pelo teclado, seleção e metadados da versão candidata.

### Русский

`v0.1.2-candidate` переводит область файлов на нативные элементы AppKit для macOS, сохраняя внешнюю SwiftUI-оболочку и существующее поведение продукта.

- Режим списка теперь использует `NSTableView`.
- Режимы файлов и предпросмотра теперь используют `NSCollectionView`.
- `ExplorerModel` остается единственным источником состояния для путей, выбора, сортировки, правил drag-and-drop, буфера обмена и фокуса панелей.
- Улучшена стабильность изменения ширины колонок, сортировки по заголовкам, выбора с клавиатуры, маршрутизации фокуса и файловой области в трехпанельном режиме.
- Исправлена прокрутка при выборе с клавиатуры: активный выбранный элемент теперь автоматически попадает в видимую область.
- Исправлена запись AppKit drag pasteboard для внутренних перетаскиваний, перетаскивания нескольких файлов и правил копирования/перемещения в стиле Finder.
- Добавлены регрессионные тесты для AppKit-файловой области, drag pasteboard, прокрутки с клавиатуры, выбора и метаданных кандидатной версии.
